"use client";

import QuestionForm from "@/components/component/Forms/QuestionForm";

// import QuestionForm from '../_component/question-form'
const Page = () => {

  const handleSubmit = (values: any) => {}

  // return <QuestionForm onSubmit={handleSubmit} />;
  return <QuestionForm/>
};

export default Page;
